import { createUser, findUserByEmail } from "../db/model/auth/auth-model"

describe("Basic queries for database", () => {

    it("simple find user in users table", async () => {
        //const result = await findUserByEmail("johndoe@example.com")
        //expect(result.data).not.toBe(null)
    })

    it("insert a new user", async () => {
        //const result = await createUser({id : "", email : "johnexamp@example.com", password : "1234567"})

        //expect(result.error).not.toBe(null)
        //expect(result.data).not.toBe(null)
    })

})